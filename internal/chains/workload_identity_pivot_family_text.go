package chains

const workloadIdentityPivotCurrentGap = "The first runnable family now emits the bounded default row set, can add exact visible patch-surface rows for safe workload fields, and can surface service-account repointing as either one named stronger target or a bounded broader lead when exact target selection would overclaim; sidecar-specific rows stay intentionally suppressed until current scope can prove honest insertion of a new sidecar rather than only changeable sidecar-related workload surfaces."

var workloadIdentityPivotHelpNotes = []string{
	"Use `harrierops-kube chains` for the overview and `harrierops-kube chains workload-identity-pivot` to run the first live family from current scope.",
	"Live row wording stays evidence-bounded: it should show what is visible, what current foothold can do, and what still needs review.",
	"Exact patch-surface rows now stay limited to safe visible workload fields the family can tie to the same workload honestly; sidecar-specific wording remains suppressed.",
	"Service-account repointing rows name one exact replacement only when current scope makes that target specific; otherwise the family keeps the lead broader.",
	"Sidecar-specific rows stay suppressed until the family can prove honest insertion of a new sidecar, not just changeable sidecar-related workload surfaces.",
}

func WorkloadIdentityPivotCurrentGap() string {
	return workloadIdentityPivotCurrentGap
}

func WorkloadIdentityPivotHelpNotes() []string {
	return append([]string(nil), workloadIdentityPivotHelpNotes...)
}
