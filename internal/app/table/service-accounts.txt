priority  service_account    workloads             power                               token_posture                                                                       why_care
high      default/fox-admin  default/fox-admin     has cluster-wide admin-like access  token auto-mount is visible on 1 attached workload; legacy token secret is visible  Identity path stands out because it has cluster-wide admin-like access, touches 1 risky workload context, is used by 1 visible workload, has a visible token path.
medium    storefront/web     storefront/web-5d4f6  can change workloads                visible workloads disable token auto-mount                                          Identity path stands out because it can change workloads, is used by 1 visible workload.
low       default/default    0 visible workloads   no strong power signal              service account allows token auto-mount; legacy token secret is visible             Identity path stands out because it has a visible token path.
