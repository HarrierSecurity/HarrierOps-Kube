priority  workload              identity                                                        exposure                  execution                                                                                                                why_care
high      default/fox-admin     runs as default/fox-admin (has cluster-wide admin-like access)  no visible exposure path  privileged; allows privilege escalation; mounts docker socket; mounts host paths; adds Linux capabilities; runs as root  Workload rises because it has cluster-wide admin-like access, privileged.
medium    build/node-debug      runs as build/default                                           no visible exposure path  uses host namespaces; mounts host paths                                                                                  Workload rises because it uses host namespaces.
medium    storefront/web-5d4f6  runs as storefront/web (can change workloads)                   no visible exposure path  no strong risky execution signal                                                                                         Workload rises because it can change workloads.
